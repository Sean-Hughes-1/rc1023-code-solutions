/* exported head */
/** Create a function that accepts and aray as the argument and returns the fist element of that array
 * return the array at the first index
 */
function head(array) {
  return array[0];
}
