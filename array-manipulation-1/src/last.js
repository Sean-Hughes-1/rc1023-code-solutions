/* exported last */
/** Create a function that accepts an array as its argument and returns the last element of that array
 * call the length property of the array and subtract by one to get the last index of the array and return the array at that index
 */
function last(array) {
  return array[array.length - 1];
}
