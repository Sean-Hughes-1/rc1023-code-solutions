/* exported getValue */
/* Create a function that accepts two arguments, an object and a key
in that function, return the key of the object */
function getValue(object, key) {
  return object[key];
}
